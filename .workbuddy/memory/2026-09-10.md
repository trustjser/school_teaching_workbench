2026-09-10 双 App 拆分（feature/dual-app-architecture）落地。

按 docs/superpowers/plans/2026-09-10-dual-app-architecture.md 执行 8 个任务，已合入分支（HEAD 7bbd016）。

关键落点：
- 固定 app target：教务端（affairs / master）与班级端（classroom / client）拆为两个独立 app。
  - Rust AppTarget::from_identifier 解析 bundle identifier，未知直接 ERR_VALIDATION 拒绝启动。
  - AppState.target 取代可变 mode 字段；state.mode() 恒等于 target.mode()。
  - 前端 apps/{affairs,classroom}/src/app-target.ts 导出编译期常量 APP_TARGET，由 mountApp(target, ...) 注入共享层。
  - 共享 getAppTarget() 不在 target 缺失时回退，重复注入不同 target 抛错。
- 共享代码边界：packages/shared 容纳两端的 types / components / stores / lib / styles。共享模块不得反向依赖端入口（scripts/check-app-boundaries.mjs 校验）。
- 运行模式不再可热切换：settings_switch_mode 命令与 switchMode store action 已删除。
- 端专属路由：apps/affairs/src/router.tsx 注册 8 条 affairs 路由；班级端 6 条。`*` 回落本端首页。
- 端专属配置：scripts/check-build-targets.mjs 校验 8 个 npm 脚本与 4 份 Tauri 配置齐全且 identifier 唯一。

双端配置：
- Vite：教务 1420 / HMR 1421，班级 1430 / HMR 1431；产物 dist/affairs、dist/classroom。
- Tauri：两套生产 + 两套 dev override，identifier 互不冲突。
- P2P API 沿用 5178 自动探测；同机双开自动错开 5178/5179。

经验 / 坑：
- 删除 src-tauri/tauri.conf.json 会让裸 cargo 编译失败（tauri-build 默认读它）。CLI 通过 --config 设置 TAURI_CONFIG 绕开，但保留默认 config 等价于教务端是更安全的妥协——正式出包仍必须用 tauri:build:affairs/classroom。
- setup_mode_for_state 是恒等函数，Task 4 要求它作为「不接受用户选择模式」的唯一落点（也便于测试），不能因为冗余就删。
- AppMode 单一来源已统一到 packages/shared/src/app-target.ts，types/enums.ts 仅 re-export。
- Rust 测试先红后绿：Task 6 的 fresh_target_defaults_* 等三组测试在签名变化前编译失败，符合计划要求。
- zsh 下 word-split 异常，批量改写 `@/...` 到 `@shared/...` 必须用 Node 脚本而非 perl -pi + zsh 变量。
- mountApp 抽到 shared 避免两端 main.tsx 复制主题/订阅样板，签名 `(target, element)` 强制入口传 target。

验收结果：
- npm run check:boundaries 通过
- npm run check:build-targets 通过
- npm run typecheck 通过
- cargo test 44/44 通过
- npm run build:affairs 与 build:classroom 都成功生成 dist/{affairs,classroom}

尚未在线做：同机双窗口 tauri:dev:* 联调、目录 / 任务 / 考勤 / 回执业务回归、生产构建产物。

## 后续小修（同日）

1. 跳转修正（commit 11fd536）：路由扁平化后页面里仍残留 `/master/...`、`/client/...` 前缀链接。
   涉及 MasterHome(4 处)、TaskDashboard(2 处)、TaskDetail(1 处)、ClientHome(3 处)、InboxPage(1 处)、TaskManage(2 处)。
   **约定：端内跳转一律使用扁平单层路径（教务 `/devices`、`/tasks/:id`；班级 `/matrix`、`/inbox`）。**
2. 班级端调整（commit f2c9560）：`apps/classroom/src/nav.ts` 移除「班级名册」入口；`TaskManage.tsx` 中 `t.source === 'broadcast'` 的任务不再显示「状态节点」编辑按钮（教务下发的节点由教务端定义，班级端只执行）。
   - `/students` 路由与 `StudentRoster.tsx` 仍保留（已无 UI 入口），待确认是否彻底删除。

3. 彻底删除班级端名册（commit 9f761cb）：`git rm apps/classroom/src/pages/StudentRoster.tsx`，router.tsx 移除 `students` 路由与 import。
   - shared 的 student 组件（StudentTable/StudentEditDrawer/StudentImportDialog/StudentPicker/StudentStatusBadge）**保留**，教务端 GradeClassManage 仍在用。
   - `scripts/check-app-boundaries.mjs` 的禁引用 token 同步换成仍然存在的另一端页面名（原 StudentRoster 已不存在）。
   - 班级端产物 363.94 kB → 354.38 kB，确认页面已不再打包。

4. 任务矩阵表格视图新增「评分 / 备注」列（commit 68b6e44）：
   - 文件：packages/shared/src/components/task/TaskMatrixView.tsx（班级端 TaskMatrix 与教务端 TaskDetail 共用）。
   - 显隐判据：`task.scoreEnabled` / `task.noteEnabled` 为真，或本班任一记录已有对应值（防历史数据被隐藏）。
   - 备注列 `max-w-[18rem] truncate` + title 悬浮全文；无值显示 `—`。
   - 仅展示，编辑仍走点击学生姓名打开 TaskRecordEditor。

5. 考勤大屏异常明细修复（commit 515c432，落在 main —— 用户已把 feature/dual-app-architecture 快进合并进 main）：
   - 根因：`ExceptionStudentRow`（Rust models.rs）与 `checkin_exception_students` 的 SQL 都漏了 `note`，前端 `types/api.ts` 早已声明 `note` → 序列化后是 undefined → 备注列恒显示 `—`。数据本身已通过 sync 同步到教务端（`checkin_repo::merge_remote` 绑定 note）。
   - 修法：Rust 模型加 `note: Option<String>` + SQL 加 `c.note AS note`；前端 AttendanceBoard 备注列改为截断 + title 悬浮。
   - 顺手消除重复：新增 `db/repo/checkin_repo::exception_students(pool, date, class_name)` 作为唯一实现，`checkin_cmd` 与 `import_export/export.rs` 都改为调用它（此前两处 SQL 完全重复，正是漏改一处才出的 bug）。
   - `late` 显示为英文：AttendanceBoard 原用手写三目映射（absent/leave），其他状态原样透出。改为统一走 `CHECKIN_STATUS_META[state].label/emoji` + `EXCEPTION_TONE` 配色表（absent→danger / leave→warning / late→orange）。
   - xlsx 导出异常 sheet 同步加「备注」列。
   - 新增 2 个 Rust 回归测试（`exception_students_returns_note_and_includes_late`、`exception_students_filters_by_class_name`），并实测「去掉 note select 后两个测试变红」确认测试有效。Rust 全量 46/46 通过。

6. 排查「班级端任务中心状态列显示未完成」：**该文案在全仓不存在**，属误报或看错列。梳理结论（无需改代码）：
   - `custom_tasks.status` 只有 4 个合法值 draft/active/closed/archived（DDL CHECK 强制），前端 `TASK_STATUS_OPTIONS` 映射为 草稿/进行中/已结束/已归档。
   - **写入端恒定 active**：班级端新建（TaskManage.tsx `status:'active'`）与教务下发接收（broadcast_cmd.rs `status:"active"`）都硬编码 active；`.sch` 导入同理。**全仓无任何把任务置为 closed/archived/draft 的代码路径**，所以该列实践上恒为「进行中」，是静态字段而非生命周期。
   - 易混淆点：`DEFAULT_NODE_TEMPLATES` 的**状态节点**标签是「未开始/进行中/已完成」，其中「进行中」与任务状态 active 的中文标签重名。节点标签展示在任务矩阵列头与网格卡片，与「任务状态」是两套东西。
   - 任务的「状态」与「学生完成度」无关；完成度存在 `task_records.node_key`，在任务矩阵里体现。
